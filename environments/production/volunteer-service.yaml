# Production values for volunteer-service

fullnameOverride: "volunteer-service"
replicaCount: 2

image:
  repository: 621996700064.dkr.ecr.us-east-1.amazonaws.com/volunteer-service
  pullPolicy: Always
  tag: e60493b1562c1151f334603af42fce50703ea46d

service:
  type: LoadBalancer
  port: 80
  targetPort: 8083
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: "nlb"

serviceAccount:
  create: true
  name: volunteer-service

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 10
  targetCPUUtilizationPercentage: 70
  targetMemoryUtilizationPercentage: 80

healthCheck:
  path: /health
  port: 8083

env:
  - name: PORT
    value: "8083"
  - name: AWS_REGION
    value: us-east-1
  - name: AWS_DYNAMODB_TABLE
    value: SolidaryTechVolunteers
  - name: OTEL_SERVICE_NAME
    value: volunteer-service

externalSecret:
  enabled: true
  secretName: volunteer-service-secrets
  data:
    - secretKey: aws-access-key-id
      remoteRef:
        key: solidarytech/production/aws/access-key-id
      envVar: AWS_ACCESS_KEY_ID
    - secretKey: aws-secret-access-key
      remoteRef:
        key: solidarytech/production/aws/secret-access-key
      envVar: AWS_SECRET_ACCESS_KEY
