{{- if .Values.serviceMonitor.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "volunteer-service.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "volunteer-service.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "volunteer-service.selectorLabels" . | nindent 6 }}
  endpoints:
  - port: http
    interval: {{ .Values.serviceMonitor.interval }}
    path: {{ .Values.serviceMonitor.path }}
{{- end }}
